

/* getch in c example */
#include<stdio.h>
#include<conio.h>

main()
{
    int ch;
    int x;
    char motDePasse [6];


    printf("Waiting for a character to be pressed from the keyboard to exit.\n");
    //_putch(0215);
    printf("%d", x);
    _putch(120);
    ch = _getche();
    printf("%d", ch);
    return 0;
}
